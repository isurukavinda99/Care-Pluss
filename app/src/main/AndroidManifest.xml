<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.careplus">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CarePlus">
        <activity android:name=".mms.Mms_updateEntry" />
        <activity android:name=".mms.Mms_viewMealPlans" />
        <activity android:name=".mms.Mms_supplementManager" />
        <activity android:name=".mms.Mms_viewMealPlan_usingDays" />
        <activity android:name=".mms.Mms_addNewPlan" />
        <activity android:name=".mms.Mms_dashboard" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>